# Airflow providers and connectors (compatible versions)
apache-airflow-providers-apache-spark>=4.0.0,<5.0.0
apache-airflow-providers-mysql>=5.0.0,<6.0.0
apache-airflow-providers-postgres>=5.0.0,<6.0.0

# Database connectors
mysql-connector-python>=8.0.0,<9.0.0

# Big data tools (simplified to avoid conflicts)
pyhive>=0.6.0
thrift>=0.13.0

# Data processing (compatible with Airflow 2.8.2)
pandas>=1.5.0,<2.0.0
numpy>=1.21.0,<2.0.0

# Utilities
requests>=2.25.0