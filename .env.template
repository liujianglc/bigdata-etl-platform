# ===========================================
# 大数据ETL平台环境配置文件
# 复制为 .env 并根据需要修改端口
# ===========================================

# PostgreSQL配置 (Airflow 和 Hive Metastore)
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow123
POSTGRES_DB=airflow
POSTGRES_PORT=5432

# Redis配置 (Airflow Celery)
REDIS_PORT=6379

# Airflow配置
AIRFLOW_WEBSERVER_PORT=8080
AIRFLOW_SCHEDULER_PORT=8974
AIRFLOW__CORE__FERNET_KEY=GLmwdKBpKXKAcz3G93PT9SRcwZuVfFhs0pyxANwYdik=
AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=false
AIRFLOW__CORE__LOAD_EXAMPLES=false

# MySQL数据源配置
MYSQL_ROOT_PASSWORD=Sp1derman
MYSQL_DATABASE=wudeli
MYSQL_USER=etl_user
MYSQL_PASSWORD=etl_pass
MYSQL_PORT=3306

# HDFS配置
CLUSTER_NAME=hadoop-cluster
HDFS_NAMENODE_HOST=namenode
HDFS_NAMENODE_PORT=9000
HDFS_UI_PORT=9870

# Hive Metastore配置
HIVE_METASTORE_USER=hive
HIVE_METASTORE_PASSWORD=hive123
HIVE_METASTORE_PORT=9083

# Spark配置
SPARK_MASTER_HOST=spark-master
SPARK_MASTER_PORT=7077
SPARK_UI_PORT=8081
SPARK_WORKER_CORES=2
SPARK_WORKER_MEMORY=2g

# ETL作业配置
ETL_SCHEDULE_CRON=0 2 * * *
ETL_TIMEZONE=Asia/Shanghai
ETL_RETRY_ATTEMPTS=2
ETL_RETRY_DELAY=10

# 数据质量配置
DATA_QUALITY_NULL_THRESHOLD=0.05
DATA_QUALITY_FRESHNESS_HOURS=25

# 监控配置
LOG_LEVEL=INFO
ENABLE_METRICS=true
ENABLE_ALERTS=true
